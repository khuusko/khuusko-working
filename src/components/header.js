import React from "react"
import Navigation from "./navigation"

export default () => (
    <div>
        <header style={{ color: `purple` }}>
            <h1>Main header</h1>
        </header>
        <Navigation />
    </div>

)